apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-data
  labels:
    app: kafka
spec:
  accessModes:
    - ReadWriteOnce   # One node can mount this at a time (fine for single-broker Kafka)
  resources:
    requests:
      storage: 2Gi